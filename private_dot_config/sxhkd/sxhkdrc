# terminal emulator
super + Return
	kitty

super + e
	nautilus

super + @space
	rofi -show combi 

super + r
	rofi -show run

super + Delete
	xkill
	
# Reload configuration
super + Escape
	pkill -USR1 -x sxhkd

Print
    deepin-screenshot

super + ctrl + q
	bspc quit

super + {t,shift + t,s,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}
